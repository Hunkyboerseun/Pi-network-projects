# ANTIBANK

ANTIBANK is a Pi Network → FIAT gateway prototype (backend + frontend).
This repo holds a testnet/mainnet-capable prototype:
- Backend (Node/Express, Prisma)
- Frontend (Next.js)
- Docker compose + prisma schema
- Public validation key for domain verification

Follow README sections to run locally or deploy to Render / Vercel.
